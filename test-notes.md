# 测试记录

## 手动测试场景

- 普通整数：范围 `1-10`，数量 `10`，确认结果均为整数且在范围内。
- 普通小数：范围 `0-1`，小数位 `2`，确认结果保留 2 位小数且在范围内。
- 不允许重复：范围 `1-3`，数量 `4`，确认显示数量不足错误。
- 参考数平衡：范围 `1-100`，参考数 `50`，数量 `10`，确认上下两侧尽量平衡。
- Seed 可复现：相同条件和相同 Seed 生成两次，确认结果一致。
- 随机分组：样本数 `11`，组名 `A组/B组/C组`，均衡分配，确认组间数量差不超过 1。
- CSV 导出：分别检查“编号 + 结果”“仅结果一列”“参数元数据 + 结果”。
- 历史记录：连续生成 11 次，确认只保留最近 10 次；点击“查看”可恢复对应结果。
- 清除历史记录：生成历史后点击“清除历史记录”，确认历史列表清空且当前结果不被删除。
- 深色模式：点击右上角“深色模式”，确认页面整体切换为低亮度深色界面；再次点击“浅色模式”可恢复。
- 语言切换：默认中文，选择 English 后确认标题、按钮、标签、错误提示和新生成内容切换为英文。
- 统计摘要：确认均值、中位数、标准差、Q1/Q3 有值且随结果更新。
- 复制：确认复制全部和复制单个结果可用。
- 响应式：确认桌面宽度和手机宽度下布局清晰、文字不重叠。

## 实现时验证结果

- 脚本语法检查：通过。
- Seed 可复现：通过。
- 普通小数和统计摘要：通过。
- 参考数平衡：通过。
- 随机分组均衡分配：通过。
- 不允许重复容量不足错误：通过。
- 历史记录最多 10 次、恢复查看：逻辑验证通过；视觉样式需浏览器手动确认。
- 清除历史记录：逻辑验证通过，历史清空后当前结果保留，按钮自动禁用。
- 深色模式切换：脚本逻辑验证通过；视觉对比度需浏览器手动确认。
- 语言切换：逻辑验证通过，默认中文、英文界面、英文错误提示和清除历史提示正常。

## 当前实现说明

- 主页面是单文件 `index.html`，内含样式和脚本。
- 项目无外部依赖。
- 当前运行环境的浏览器自动化拦截了本地页面打开，因此视觉检查需直接用浏览器打开 `index.html` 后确认。
